# 硬件采购与制作交接（第一缸温度监控 MVP）

## 任务范围

为一只水猎人底滤缸建立**纯低压、只监控、不控制市电**的温度监控原型。主缸尺寸为 80 × 50 × 55 cm；鱼为奈及利亚圆红点狗头 `Tetraodon pustulatus`。现有加热棒维持独立运行，设定 26°C；夏季实际水温通常 26–27°C，冬季加热后约 25°C。

本交接不包含阿里云账号配置或固件编写。MVP 只做双探头温度监控与 Bark 告警，不接入任何 220V 控制。

## 最终确认硬件合同

- 主控：微雪 Waveshare `ESP32-S3-DEV-KIT-N16R8-M`，2 块。
  - `N16R8` 为 16MB Flash + 8MB PSRAM；`-M` 为已焊排针。
  - 不选 N32R16V，也不选 U/UM 外接天线版。
- 探头：DFRobot `KIT0021` 全防水 DS18B20 温度传感器套件，3 套。
  - 实际安装 2 支：主缸探头是安全报警依据；底滤上水泵/回水仓探头仅作循环诊断。
  - 第 3 支为备件，不先安装。
  - 每套包含 DFR0198 探头、DFR0055 转接板和 Gravity 线。最终 WAGO 方案只用探头本体和外置上拉电阻；DFR0055 转接板及其跳帽保留为备用，**不得与外置 4.7kΩ 上拉并用**。
  - DFRobot 官方同时列出两种线序：Type A 为红 VCC、黑 GND、黄 DATA；Type B 为红 VCC、黄 GND、绿 DATA。必须以到货标签/说明核对，不得按颜色猜线。
- 原型：`MB-102` 830 孔面包板 1 块；2.54mm 杜邦线 20cm、40P 三排组合 1 套；4.7kΩ 1/4W 金属膜电阻 1 包。
- 连接：WAGO `221-413` 三孔连接器 10 个，作为本 MVP 的无焊接拼接与汇合方案。
  - 两支探头接入时，DATA、3V3、GND 各用一个 WAGO 汇合两根 RVV 芯线和一根到面包板的短杜邦线。
  - 如需为两支探头分别拼接延长线，每支另用 3 个 WAGO，分别处理 VCC、GND、DATA；总计最多使用 9 个。
- 延长：RVV 三芯 0.3平方线 5m。这里的 `0.3平方` 是每芯导体截面积 0.3mm²，不是线径 0.3mm。按实际走线路径裁剪，接头必须留在防水盒内或其他干燥位置，不得入水。
- 盒体与安装：IP65/IP67 ABS 防水接线盒约 `200×120×75mm`，2 个；M12/PG7 防水接头 10 个；`3-12 / 4-12 / 4-20mm` 阶梯钻头套装 1 套；164pcs 彩色热缩管 1 盒；可调固定座/扎带座（黑色 20 个）1 包。
  - 第一个盒子用于当前低压原型；第二个保持未开孔，作为备件或重新布线时的替换盒。
  - 防水接头和 USB-C 面板件的实际开孔尺寸以到货实物为准，先量再逐级扩孔。
- USB：USB-C 防水面板延长线 0.3m，标题含 `2.0USB-C`、公对母、数据线，1 条。盒内 USB-C 公头接 ESP32，盒外 USB-C 母口接用户现有 USB-A 转 USB-C 数据线，用于供电、刷机和串口。
  - 用户已有 USB-A 5V 2A 以上充电器和 USB-A 转 USB-C 数据线 1–1.5m 共 2 根，不另购充电器和线。
  - 防尘盖只在接口未插线时盖上；它不使整个系统可浸水。盒体和电源仍放在底柜外侧干燥处。
- 工具：ANENG `616` 万用表 1 个，只用于低压连续性与电压检查；Pro'sKit 宝工 `8PK-3001D` 手动剥线钳 1 个。RVV 0.3平方导体可先在 0.6mm 档位试剥并检查铜丝完整。

已有：独立温度计（校准）、漏电保护小开关、探头固定件和 3M 固定胶。现有加热棒继续独立运行。

## 固定接线合同（只含低压）

| OneWire 节点 | ESP32-S3 引脚 | 连接方式 |
|---|---|---|
| VCC | `3V3` | 两支已安装探头的 VCC 并联；只用 3.3V，不接 5V |
| GND | `GND` | 两支已安装探头的 GND 并联 |
| DATA | `GPIO4` | 两支已安装探头的 DATA 并联到同一总线 |
| 上拉 | `GPIO4` ↔ `3V3` | **仅一只** 4.7kΩ、1/4W 金属膜电阻 |

固件当前固定使用 GPIO4。每支 DFRobot `KIT0021` 探头都具有独立 64-bit ROM 地址；首次串口扫描后，再将主缸和底滤角色按 ROM 固定。

WAGO `221-413` 对细绞线的额定范围为 0.14–4mm²；RVV 0.3平方满足该范围。探头内芯线未见实物前不得假定其截面积：先在**不通电**状态下试夹并轻拉确认。若夹不牢、铜丝外露或线径低于端子要求，停止该连接方式，保留 DFR0055 转接板并反馈实物照片，不得硬塞、焊接或把接头放入水中。

## 到货后的制作顺序

1. **清点与干态检查**：核对型号、数量、探头线序、USB-C 面板件和防水接头尺寸；不对任何盒体开孔。
2. **桌面上电**：ESP32-S3 直接以现有 USB-A 转 USB-C 数据线连接电脑，确认出现串口、可烧录最小程序；不先装入盒体或底柜。
3. **做三条低压母线**：面包板上分别建立 3V3、GND、GPIO4(DATA)。先用万用表确认 3V3/GND 没有短路，再接 USB。
4. **接第一支 KIT0021**：以到货核实的 VCC/GND/DATA 线序接入；DATA 与 3V3 间只接一只 4.7kΩ 上拉。记录 ROM 地址和温度。
5. **接第二支 KIT0021**：并联到同一 3V3、GND、GPIO4(DATA) 总线，不增加第二只上拉电阻。记录第二个 ROM 地址。
6. **校准**：两支探头与独立温度计在同杯/同一稳定水流中静置 15 分钟，记录偏差；异常探头不下缸。
7. **干态运行**：桌面连续运行至少 2 小时，确认没有重启、双探头读数稳定、USB/线缆无发热。
8. **做 RVV 拼接与盒体布局**：在干燥盒内用 WAGO 拼接；所有 WAGO 杠杆必须完全压下，透明壳内可见导体。先做无电的连续性检查，再做低压上电检查。
9. **开孔与装盒**：按实物量出的防水接头和 USB-C 面板尺寸逐级开孔；探头 RVV 线经 M12/PG7 入盒，USB-C 面板件用于外部刷机/供电。所有线缆留滴水弯和应力释放。
10. **下缸与观察**：主缸探头放中下层、避开直冲水流/加热棒；底滤探头远离加热棒与泵体。先只监控运行 48–72 小时。

## 风险与边界

- 底滤仓水位变化、泵停或加热棒附近局部温升会令底滤读数偏离主缸，因此它不能替代主缸安全探头。
- 面包板、杜邦线、WAGO 与无焊接拼接是 MVP 原型，不等同于可长期无人值守的成品布线。后续成品化需另行审批，不在本次任务内。
- 任何检查和制作都只在 USB 5V/3.3V 低压侧完成。万用表不用于用户处理市电；不改动加热棒、回水泵、插排、漏电保护器或原有 220V 布线。

## 新任务开场提示

“请读取 `HANDOVER_HARDWARE_20260716.md`，按最终确认的 DFRobot KIT0021、WAGO 221-413、RVV 三芯 0.3平方和 USB-C 面板数据线继续第一缸 ESP32-S3 温控制作。先从桌面上电、识别串口和单探头接线开始，不接入任何 220V 控制。”
